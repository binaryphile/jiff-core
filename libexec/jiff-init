#!/usr/bin/env bash
# Usage: jiff init

source std.sh
source jiff.sh

Verb="init"

main() {
  jiff::run_subtask "${Verb}" "$@"
}

usage() {
  printf "Usage: jiff %s TASK\n" "${Verb}"
}

return 0 2>/dev/null || true
std::strict_mode on
main "$@"
