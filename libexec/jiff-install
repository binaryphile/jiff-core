#!/usr/bin/env bash
# Usage: jiff install [package]
# Summary: Install and configure a system package

source stdlib
source jifflib
source platformlib

strict_mode_on

package="${1}"

run_and_exit_if_is_jiff_task "${package}"
install_package "${package}"
