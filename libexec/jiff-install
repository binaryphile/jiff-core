#!/usr/bin/env bash
# Usage: jiff install [package]
# Summary: Install and configure a system package

source stdlib
source jifflib
source platformlib

strict_mode on

package="${1}"

run_and_exit_if_is_jiff_task "${package}-install"
install_package "${package}"
