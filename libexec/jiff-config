#!/usr/bin/env bash
# Usage: jiff config PACKAGE

source std.sh
source jiff.sh

Verb="config"

main() {
  jiff::run_subtask "${Verb}" "$@"
}

usage() {
  printf "Usage: jiff %s PACKAGE\n" "${Verb}"
}

return 0 2>/dev/null || true
std::strict_mode on
main "$@"
