#!/usr/bin/env bash
# Usage: jiff add TASK

source sh.sh
source jiff.sh

Verb="add"

main() {
  jiff::run_subtask "${Verb}" "$@"
}

usage() {
  printf "Usage: jiff %s TASK\n" "${Verb}"
}

return 0 2>/dev/null || true
sh::strict_mode on
main "$@"
